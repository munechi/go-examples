# Hello world!

軽量フレームワーク `Chi` を使用する場合。

## サーバー起動

```bash ln=false
go run hello-world-chi/main.go
```

## テスト

```bash ln=false
curl http://localhost:8080/hello
```

## 出力

```text ln=false
{"message":"Hello world!"}
```
